---
import Navigation from './Navigation.astro';

const currentPath = Astro.url.pathname;
const base = import.meta.env.BASE_URL;
---

<header class="border-b border-terminal-greenDim bg-terminal-black sticky top-0 z-50">
  <div class="container mx-auto px-4 pt-4 pb-4 md:pb-2 flex items-baseline justify-between gap-4">
    <!-- Logo/Brand -->
    <a href={`${base}/`} class="flex items-baseline gap-2 no-underline hover:no-underline flex-shrink-0">
      <span class="text-terminal-green text-base font-bold">></span>
      <span class="text-terminal-green text-base font-bold">
        The Copilot Stack
      </span>
      <span class="text-terminal-green text-base animate-pulse">_</span>
    </a>

    <!-- Navigation -->
    <Navigation currentPath={currentPath} />
  </div>
</header>

<style is:global>
  @keyframes pulse {
    0%, 100% {
      opacity: 1;
    }
    50% {
      opacity: 0;
    }
  }

  .animate-pulse {
    animation: pulse 1s ease-in-out infinite;
  }
</style>
